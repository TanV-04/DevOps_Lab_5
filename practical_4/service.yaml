apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  selector:
    app: myapp
  ports:
    - protocol: TCP
      port: 5000 # 👈 service port
      targetPort: 5000 # 👈 forward to container port
      nodePort: 30007 # 👈 external port
