apiVersion: v1
kind: Service
metadata:
  name: apache2-service
  namespace: apache-server
  labels:
    app: apache2
spec:
  type: NodePort
  selector:
    app: apache2
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30090
    name: http
